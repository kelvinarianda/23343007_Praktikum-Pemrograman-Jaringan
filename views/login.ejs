<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Login | MarketWave Financial Terminal</title>
  
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    :root {
      --bg-dark: #0a0f1e;
      --card-bg: #161e31;
      --accent-gold: #fbbf24;
      --pure-white: #ffffff;
      --info-cyan: #22d3ee;
      --border-color: rgba(251, 191, 36, 0.4); /* Border sedikit lebih terang */
    }

    body {
      background-color: var(--bg-dark);
      color: var(--pure-white);
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background-image: radial-gradient(circle at 10% 20%, rgba(251, 191, 36, 0.08) 0%, transparent 40%);
    }

    .premium-title {
      font-family: 'Playfair Display', serif;
      color: var(--accent-gold);
      letter-spacing: 2px;
      text-transform: uppercase;
      font-weight: 800;
    }

    .login-container {
      max-width: 900px;
      width: 100%;
      padding: 20px;
    }

    .card {
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.7);
      overflow: hidden;
    }

    .benefit-section {
      background: rgba(0, 0, 0, 0.3);
      padding: 40px;
      border-right: 1px solid var(--border-color);
    }

    .form-section {
      padding: 40px;
    }

    /* Mengubah warna label form menjadi Gold agar lebih nampak */
    .form-label-gold {
      font-size: 0.8rem;
      color: var(--accent-gold);
      text-transform: uppercase;
      font-weight: 700;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }

    .form-control {
      background-color: #0f172a;
      border: 1px solid var(--border-color);
      color: white;
      padding: 12px;
    }

    .form-control:focus {
      background-color: #0f172a;
      border-color: var(--accent-gold);
      color: white;
      box-shadow: 0 0 8px rgba(251, 191, 36, 0.3);
    }

    .btn-gold {
      background-color: var(--accent-gold);
      color: #000;
      font-weight: 800;
      border: none;
      padding: 14px;
      transition: all 0.3s;
    }

    .btn-gold:hover {
      background-color: #f59e0b;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(251, 191, 36, 0.4);
    }

    /* Mengubah teks deskripsi abu-abu menjadi Gold terang / Putih bersih */
    .text-gold-light {
      color: rgba(251, 191, 36, 0.85);
      font-size: 0.85rem;
      line-height: 1.6;
    }

    .benefit-item {
      display: flex;
      align-items: flex-start;
      margin-bottom: 25px;
    }

    .benefit-icon {
      color: var(--accent-gold);
      margin-right: 15px;
      font-size: 1.3rem;
      line-height: 1;
    }

    .demo-box {
      background: rgba(34, 211, 238, 0.05);
      border-radius: 8px;
      padding: 15px;
      border: 1px solid var(--info-cyan);
      margin-top: 30px;
    }

    .demo-text {
      color: var(--info-cyan);
      font-family: monospace;
      font-size: 0.85rem;
    }
  </style>
</head>
<body>

  <div class="login-container">
    <div class="card">
      <div class="row g-0">
        <div class="col-lg-6 benefit-section d-none d-lg-block">
          <h2 class="premium-title mb-4">MarketWave</h2>
          <h5 class="mb-4" style="color: var(--info-cyan); font-weight: 700;">KEUNTUNGAN TERMINAL:</h5>
          
          <div class="benefit-item">
            <span class="benefit-icon">✔</span>
            <div>
              <strong style="color: var(--pure-white);">Smart AI ChatBot</strong>
              <p class="text-gold-light">Asisten investasi pribadi untuk konsultasi strategi pasar secara real-time.</p>
            </div>
          </div>

          <div class="benefit-item">
            <span class="benefit-icon">✔</span>
            <div>
              <strong style="color: var(--pure-white);">Daily Email Reports</strong>
              <p class="text-gold-light">Rekap harian otomatis harga USD/IDR, BTC, IHSG, dan XAU langsung ke email Anda.</p>
            </div>
          </div>

          <div class="benefit-item">
            <span class="benefit-icon">✔</span>
            <div>
              <strong style="color: var(--pure-white);">Premium Dashboard</strong>
              <p class="text-gold-light">Visualisasi data interaktif dengan sinyal pasar yang akurat.</p>
            </div>
          </div>

          <div class="demo-box">
            <div class="small fw-bold mb-2" style="color: var(--info-cyan); text-transform: uppercase;">Akses Demo:</div>
            <div class="demo-text">• admin@marketbot.local / admin123</div>
            <div class="demo-text">• user@marketbot.local / user123</div>
          </div>
        </div>

        <div class="col-lg-6 form-section">
          <div class="mb-4">
            <h3 class="fw-bold text-white">Member Login</h3>
            <p style="color: var(--accent-gold); font-size: 0.9rem;">Silakan masuk untuk mengakses fitur premium.</p>
          </div>

          <% if (error) { %>
            <div class="alert alert-danger py-2 small" style="background: rgba(220, 53, 69, 0.2); border-color: #dc3545; color: #ff8e98;">
              <%= error %>
            </div>
          <% } %>

          <form method="POST" action="/login">
            <div class="mb-3">
              <label class="form-label-gold">Alamat Email</label>
              <input
                type="email"
                name="email"
                class="form-control"
                placeholder="email@example.com"
                value="<%= (form && form.email) ? form.email : '' %>"
                required
              />
            </div>

            <div class="mb-4">
              <label class="form-label-gold">Kata Sandi</label>
              <input
                name="password"
                type="password"
                class="form-control"
                placeholder="••••••••"
                required
              />
            </div>

            <button class="btn btn-gold w-100 mb-3">MASUK KE TERMINAL</button>

            <div class="d-flex justify-content-between align-items-center mt-4 pt-3 border-top border-secondary">
              <a href="/" class="btn btn-sm btn-outline-gold px-3">Kembali</a>
              <div class="small" style="color: white;">
                Belum punya akun? <a href="/register" class="text-decoration-none fw-bold" style="color: var(--accent-gold)">Daftar Sekarang</a>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

</body>
</html>