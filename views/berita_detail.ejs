<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= berita.judul %> | MarketWave Terminal</title>
  
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@700;800&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    :root {
      --bg-dark: #0a0f1e;
      --card-bg: #161e31;
      --accent-gold: #fbbf24;
      --pure-white: #ffffff;
      --info-cyan: #22d3ee;
      --border-color: rgba(251, 191, 36, 0.3);
    }

    body {
      background-color: var(--bg-dark);
      color: var(--pure-white);
      font-family: 'Inter', sans-serif;
      line-height: 1.8;
    }

    /* Top Navigation */
    .top-nav {
      background: rgba(10, 15, 30, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--border-color);
      position: sticky;
      top: 0;
      z-index: 1000;
      padding: 12px 0;
    }

    /* Article Styling */
    .article-container {
      max-width: 850px;
      margin: 40px auto;
    }

    .hero-image {
      width: 100%;
      height: 450px;
      object-fit: cover;
      border-radius: 16px;
      border: 1px solid var(--border-color);
      box-shadow: 0 20px 40px rgba(0,0,0,0.5);
    }

    .category-tag {
      color: var(--accent-gold);
      font-weight: 700;
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      display: block;
      margin-bottom: 10px;
    }

    .article-title {
      font-family: 'Playfair Display', serif;
      font-size: 2.8rem;
      font-weight: 800;
      line-height: 1.2;
      color: var(--pure-white);
      margin-bottom: 20px;
    }

    .article-meta {
      display: flex;
      align-items: center;
      gap: 15px;
      border-bottom: 1px solid var(--border-color);
      padding-bottom: 25px;
      margin-bottom: 30px;
    }

    .meta-item {
      color: var(--info-cyan);
      font-size: 0.85rem;
      font-weight: 500;
    }

    .article-summary {
      font-size: 1.25rem;
      color: var(--accent-gold);
      font-weight: 500;
      line-height: 1.6;
      border-left: 4px solid var(--accent-gold);
      padding-left: 20px;
      margin-bottom: 35px;
      font-style: italic;
    }

    .article-body {
      font-size: 1.15rem;
      color: rgba(255, 255, 255, 0.9);
      text-align: justify;
    }

    .article-body p {
      margin-bottom: 25px;
    }

    /* Buttons */
    .btn-gold-outline {
      border: 1px solid var(--accent-gold);
      color: var(--accent-gold);
      font-weight: 600;
      transition: all 0.3s;
    }

    .btn-gold-outline:hover {
      background: var(--accent-gold);
      color: #000;
    }

    .btn-gold {
      background: var(--accent-gold);
      color: #000;
      font-weight: 700;
      border: none;
    }

    @media (max-width: 768px) {
      .article-title { font-size: 2rem; }
      .hero-image { height: 250px; }
    }
  </style>
</head>
<body>

  <nav class="top-nav mb-4">
    <div class="container d-flex justify-content-between align-items-center">
      <a href="/" class="btn btn-sm btn-gold-outline px-3">← BERANDA</a>
      <div class="d-flex align-items-center gap-3">
        <% if (!pengguna) { %>
          <a href="/login" class="btn btn-sm btn-gold px-4">LOGIN</a>
        <% } else { %>
          <span class="small text-white-50 d-none d-md-inline">Terminal Aktif: <strong><%= pengguna.nama %></strong></span>
          <a href="/dashboard" class="btn btn-sm btn-gold px-4">DASHBOARD</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container">
    <article class="article-container">
      
      <header class="mb-5">
        <span class="category-tag">Market Analysis</span>
        <h1 class="article-title"><%= berita.judul %></h1>
        
        <div class="article-meta">
          <div class="meta-item">
            <span class="opacity-50 text-white">Oleh:</span> MarketWave Editorial
          </div>
          <div class="meta-item">
            <span class="opacity-50 text-white">Tanggal:</span> <%= new Date(berita.createdAt).toLocaleDateString("id-ID", { day: 'numeric', month: 'long', year: 'numeric' }) %>
          </div>
          <div class="meta-item">
            <span class="opacity-50 text-white">Waktu:</span> <%= new Date(berita.createdAt).toLocaleTimeString("id-ID", { hour: '2-digit', minute: '2-digit' }) %> WIB
          </div>
        </div>

        <img src="<%= berita.gambar %>" alt="<%= berita.judul %>" class="hero-image mb-5">
      </header>

      <% if (berita.ringkasan) { %>
        <div class="article-summary">
          "<%= berita.ringkasan %>"
        </div>
      <% } %>

      <div class="article-body">
        <%- berita.isi %>
      </div>

      <footer class="mt-5 pt-4 border-top border-secondary text-center">
        <p class="text-white-50 small mb-4">© 2025 MarketWave Financial Terminal. Seluruh informasi bersifat edukatif.</p>
        <a href="/" class="btn btn-outline-secondary btn-sm">Kembali ke Indeks Berita</a>
      </footer>

    </article>
  </div>

</body>
</html>